package common.command;


public enum CommandType {
    HELP,
    INFO,
    SHOW,
    INSERT,
    UPDATE,
    REMOVE_KEY,
    CLEAR,
    REMOVE_LOWER,
    REPLACE_IF_GREATER,
    REMOVE_GREATER_KEY,
    AVERAGE_OF_PRICE,
    GROUP_COUNTING_BY_ID,
    FILTER_LESS_THAN_PRICE,
    EXECUTE_SCRIPT,
    LOGIN,
    EXIT;
}